#!/bin/csh -f

cd `dirname $0`

if ( ${?MGICONFIG} == 0 ) then
	setenv MGICONFIG /usr/local/mgi/live/mgiconfig
endif

source ${MGICONFIG}/master.config.csh

setenv MIGRATIONDIR	${DBUTILS}/mgidbmigration/MGI3.5

setenv CREATEDBY	1000
setenv PUBLIC_VERSION	"MGI 3.52"

