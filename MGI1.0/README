
Tables where order has changed:

BIB_Refs
RI_RISet
GXD_Index

Other changes:

PRB_Probe._Source_key changed to NOT allow NULLS.
All NULL values will be assigned to "Not Applicable" key value in PRB_Source.

PRB_Source.ageMin new column
PRB_Source.ageMax new column

MRK_History.creation_date data moved to MRK_History.event_date.
MRK_History.modification_date field added.

HMD tables (see spec)

Probe Library (PRB_Source)

To load MGD_DEV mgd_new:

1. ./schema/mgd_drop_tables (removed PRB and HMD tables)

2. run columns.sql MGD_DEV mgd_new to create tables
-- need to debug yet -- 

3. run ./schema/mgd... to rebuild indexes, etc..

4. Lori reloads PRB and HMD tables

To migrate the data:

1.  kelso> cd $SYBASE/admin/migration/MGI1.0

2.  MGI.sh MGD_DEV mgd_new

3.  If you want to migrate portions:

	a.  Homology.sh MGD_DEV mgd_new
	b.  Marker.sh MGD_DEV mgd_new
	c.  Misc.sh MGD_DEV mgd_new
	d.  Probe.sh MGD_DEV mgd_new
	e.  Strains.sh MGD_DEV mgd_new

