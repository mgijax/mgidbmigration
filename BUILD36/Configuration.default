#!/bin/csh -f

if ( ${?MGICONFIG} == 0 ) then
	setenv MGICONFIG /usr/local/mgi/live/mgiconfig
endif

source ${MGICONFIG}/master.config.csh

setenv BE_SERVER_PATH  /net/mtdoom-g/vol/nardol/usrlocalmgi/live
setenv BE_SERVER_SCHEMADIR      ${BE_SERVER_PATH}/dbutils/snp_be/snpdbschema

setenv BE_SERVER_PERMSDIR ${BE_SERVER_PATH}/dbutils/snp_be/snpdbperms

